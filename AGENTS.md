# sage-workflows agent notes

This repository uses `jj`; do not assume git-style workflows. Use Conventional Commits.

## Issue tracker

Issues are managed with `gh` against `deevus/sage-workflows`. See `docs/agents/issue-tracker.md`.

## Triage labels

Default five-label vocabulary. See `docs/agents/triage-labels.md`.

## Domain docs

Read root `CONTEXT.md` and `docs/adr/` when present. See `docs/agents/domain.md`.
